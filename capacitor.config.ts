import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SmartQueue - Web Master',
  webDir: 'dist'
};

export default config;
